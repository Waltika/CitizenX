// This content script is a placeholder for potential future functionality.
// The annotation UI is now rendered in the side panel via an iframe.
console.log("Content script initialized (index.ts)");

// Optionally, we can add logic here to communicate with the side panel or perform other page-specific tasks.